on:
  push:
    branches:
      - master



jobs:
  build:
    runs-on: ubuntu-larest


    steps:
      - name: 'Testing github actions'
        run: pwd
      
      - name: 'Testing Printing'
        run: echo Adel
